server {
    listen 80;
    server_name localhost;

    # React 앱의 정적 파일을 제공할 디렉터리
    root /usr/share/nginx/html;
    index index.html;

    # SPA (Single Page Application) 라우팅 처리
    location / {
        try_files $uri /index.html;
    }

    # 에러 페이지 설정 (선택 사항)
    error_page 404 /index.html;
}
